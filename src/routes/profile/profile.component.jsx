import ProfileConfigs from "../../components/profile-configs/progile-configs.component";
import "./profile.component.style.scss";

const UserProfile = () => {

    return (
        <ProfileConfigs />
    );
};

export default UserProfile;
